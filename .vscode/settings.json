{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "usertable",
            "database": "${workspaceFolder:quote-generator}/user.db"
        }
    ]
}